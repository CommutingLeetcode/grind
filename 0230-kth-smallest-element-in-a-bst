# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def kthSmallest(self, root: Optional[TreeNode], k: int) -> int:
        # iterative DFS to optimize the code to O(k) time complexity
        stack = []
        curr = root
        while(stack or curr):
            while(curr):
                stack.append(curr)
                curr = curr.left
            curr = stack.pop()
            # process the node
            k -= 1
            if k == 0:
                return curr.val
            curr = curr.right
    # def kthSmallest(self, root: Optional[TreeNode], k: int) -> int:
    #     def inOrder(root):
    #         nonlocal arr
    #         if not root:
    #             return
    #         inOrder(root.left)
    #         arr.append(root.val)
    #         inOrder(root.right)
    #     arr = []
    #     inOrder(root)
    #     return arr[k - 1]

'''
This problem shows the importance of knowing the iterative approach to inorder traversal of BST. We could optimize the time complexity from O(n) to O(k) by stopping the code whenever kth smallest element is found
'''
